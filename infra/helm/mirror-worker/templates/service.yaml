apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirror-worker.fullname" . }}
  labels:
    {{- include "mirror-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mirror-worker.selectorLabels" . | nindent 4 }}
