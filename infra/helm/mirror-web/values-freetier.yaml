replicaCount: 1

autoscaling:
  enabled: false

pdb:
  enabled: true
  minAvailable: 0

nodeSelector:
  kubernetes.io/arch: arm64

resources:
  requests:
    memory: 512Mi
    cpu: 250m
  limits:
    memory: 1Gi
    cpu: 1000m

serviceMonitor:
  enabled: false
